import type { Transform } from "@/components/boundary-box";

const DEFAULT_TRANSFORM: Transform = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
  scale: 1,
  normX: 0,
  normY: 0,
};

export { DEFAULT_TRANSFORM };
